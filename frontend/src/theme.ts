import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#d93f3f", // Set your primary color
    },
    secondary: {
      main: "#fafafa", // Set your secondary color
    },
    // Add more colors or customize existing ones as needed
  },
  // Customize other theme options, such as typography, spacing, etc.
});

export default theme;
